#!/usr/bin/env bash
set -euo pipefail

# Git will run hooks from repository root when core.hooksPath is set.
# Invoke the compression script; any failure will block the commit with a helpful message.
scripts/compress-staged-images.sh
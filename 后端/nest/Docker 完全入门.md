你花了一整天在本地搭建好项目环境，所有功能都运行完美。但当需要部署到服务器或分享给同事时，却接连遇到"依赖缺失"、"版本冲突"、"环境不兼容"等问题...

Docker 就是来解决这些痛点的。

## Docker 是什么？
Docker 是一个开源的**应用容器引擎**，它允许开发者将应用及其所有依赖（库、配置文件、运行时等）打包到一个**标准化的单元**中，这个单元被称为 **Docker 镜像 (Image)**。

可以把它形象地理解为一个“**标准化的软件集装箱**”。

Docker 镜像确保了软件的分发和运行标准。无论是在你的笔记本电脑、测试服务器还是生产环境，只要安装了 Docker，这个“软件集装箱”——即 **Docker 容器 (Container)**（镜像的运行实例）—— 就能**一键启动**，并且表现得**完全一致**。



## Docker 的核心优势
**🎯**** **彻底解决环境差异问题，确保开发、测试、生产环境的高度一致性。

**⚡**** **相比传统虚拟机，Docker 容器启动速度达到秒级，资源占用更少，运行效率更高。

**🔒**** **每个应用运行在独立的容器中，互不影响，可以安全地运行不同版本依赖的服务。

**🚀**** **一次构建，处处运行。简化部署流程，实现应用的快速迭代与弹性伸缩。

**☁️**** **主流云平台（AWS、阿里云、腾讯云等）都提供容器服务，可直接部署 Docker 镜像。



## 虚拟机 VS 容器
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748099781568-2b64adfa-00d7-462e-8132-16363126fc92.png)

| 特性 | 虚拟机 (VM) | 容器 (Container) |
| :--- | :--- | :--- |
| **隔离性** | 强（硬件级隔离） | 中等（进程级隔离） |
| **资源占用** | 高（需分配固定资源） | 低（动态共享资源） |
| **启动速度** | 慢（分钟级） | 快（秒级） |
| **存储占用** | 大（GB 级） | 小（MB 级） |
| **系统要求** | 需完整操作系统 | 共享宿主机内核 |
| **性能开销** | 较高 | 接近原生性能 |


虚拟机和容器最本质的区别就是虚拟化的层次不同。

虚拟机是把**整个操作系统都虚拟出来**，就像你在一台电脑上安装了好几个完整的操作系统，每个都有自己的内核、驱动什么的。

而容器呢，它只是把**应用程序隔离开**，大家还是共用同一个操作系统内核。

这就导致了很明显的差异：虚拟机启动要好几分钟，因为要启动整个操作系统；容器几秒钟就能起来，因为操作系统已经在那了。

资源消耗上，虚拟机也要重得多，一个虚拟机可能要占几个 G 的内存，而容器可能只要几十 M。

不过虚拟机的隔离性更好，一个虚拟机崩了不会影响其他的，而容器因为共享内核，理论上安全性会差一点。

所以选择哪个主要看你的需求，如果要快速部署和扩展（微服务很适合），容器更合适；如果要强隔离或者运行不同操作系统，虚拟机更好。



## Docker 核心概念
### 镜像（Image）
它是一个**只读的模板**，就像一张“光盘”或者一个“安装包”。里面包含了运行某个软件所需的所有东西：代码、运行时库、环境变量、配置文件等。

镜像是分层的，每一层都是一些文件变更的记录，这种设计让镜像构建和分发更高效。

你可以自己创建镜像，也可以从公共或私有的仓库 (Repository) 下载别人做好的镜像。

### 容器（Container）
镜像是静态的模板，而容器是**镜像运行起来的实例**。你可以把镜像看成类 (Class)，容器就是这个类的一个对象 (Instance)。

容器被启动后，就在一个隔离的环境中运行，有自己的文件系统、网络空间和进程空间。

容器可以被启动、开始、停止、删除。

### 仓库（Repository）
就是存放镜像的地方，好比代码仓库（像 GitHub）。

最著名的公共仓库是 **Docker Hub**，上面有成千上万的官方和社区贡献的镜像（比如 Nginx、MySQL、Redis 等）。你也可以搭建自己的私有仓库。

### Dockerfile
这是一个**文本文件**，里面包含了一系列指令，用来告诉 Docker 如何一步步自动构建一个镜像。比如，基于哪个基础镜像、复制哪些文件进去、安装什么依赖、容器启动时默认执行什么命令等等。

### 数据卷（Volume）
容器默认是无状态的，也就是说容器删除后，里面产生的数据也就没了。数据卷就是用来**持久化存储数据**的。

它可以让数据独立于容器的生命周期存在，即使容器被删除了，数据卷里的数据依然保留。也可以用于容器间共享数据。

### 网络（Network）
Docker 允许容器之间、容器与宿主机、容器与外部网络之间进行通信。它提供了多种网络模式（如桥接模式、主机模式）来满足不同场景的需求。



## 安装 Docker
首先需要安装 Docker，直接从[官网](https://www.docker.com/products/docker-desktop/)下载 Docker Desktop 就行：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1747929949258-2da4968a-793c-4b9e-b33b-728cc819f7d9.png)

Docker Desktop 提供了一个图形化界面，方便我们查看和管理本地的镜像（Images）、运行中的容器（Containers） 和数据卷（Volumes） 等。

安装完成命令行输入 `docker -h` 看下 docker 命令是否可用：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748312091716-490b589d-bcc9-4418-b7b7-ed5b20af40d7.png)



## 🌰 拉取并运行一个 Nginx 镜像
1. **搜索镜像**：在 Docker Desktop 的搜索框里输入 `nginx`，它会从 Docker Hub 搜索。找到官方的 `nginx` 镜像：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1747972703958-24d65b26-7556-44e0-9804-7024ad544f9f.png)

2. **拉取镜像 (Pull)**：找到官方的 `nginx` 镜像，点击旁边的 "Pull" 按钮。选择一个标签（tag），比如 `latest` 表示最新版，然后等待下载完成。 

下载完成后，你可以在 "Images" 列表里看到这个 `nginx` 镜像：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100122314-1b49e6ee-d762-46df-8e60-4c351aa8009e.png)

3. **运行镜像 (Run)**：在 "Images" 列表里找到 `nginx` 镜像，点击它右侧的 "Run" 按钮。这时会弹出一个配置窗口：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100151971-89b09d77-8ece-4125-853c-7a9cf0175b79.png)

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100155471-d052c3de-6885-41e9-8347-cfde91d8ea36.png)

解释下这些配置：

+ **Container name (容器名称)**：给你的容器起个名字，比如 `nginx-test`。不填的话 Docker 会自动生成一个随机名字。
+ **Ports (端口映射)**：
    - Nginx 在容器内部默认监听 `80` 端口。
    - 我们需要把宿主机的一个端口（比如 80）映射到容器的 `80` 端口。这样，我们访问宿主机的 `80` 端口时，请求就会被转发到容器内的 Nginx 服务。格式是 `宿主机端口:容器端口`，例如 `80:80`。
+ **Volumes (数据卷挂载)**：
    - 这是实现数据持久化和内容替换的关键。Nginx 默认的网站根目录在容器内的 `/usr/share/nginx/html`。
    - 我们可以把宿主机的一个目录 `/tmp/test `挂载到容器的 `/usr/share/nginx/html` 目录。
    - **Host path (宿主机路径)**：填写自己本地的目录。
    - **Container path (容器内路径)**：填写 Nginx 镜像预期的内容路径，通常是 `/usr/share/nginx/html`。
+ **Environment variables (环境变量)**：可以为容器设置一些环境变量，供容器内运行的程序读取。这里 Nginx 暂时用不到。
4. **启动容器**：配置好之后，点击 "Run" 按钮：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100377145-82ae42ed-0b8b-4742-93e5-3abd5d5a571b.png)

这样 Nginx 容器已经在运行了。



## 验证数据卷挂载
1. **在宿主机创建文件**：

先进入/tmp/test 目录下：

```bash
cd /tmp/test

# 如果没有这个目录，先创建
mkdir /tmp/test
```

在这个目录添加 index.html：

```bash
echo "hello" > ./index.html
```

2. **访问 Nginx 服务**：打开浏览器，访问 `http://localhost:80`：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100496583-8e8fd8e3-4f3a-436f-9159-3efbc065ddc4.png)

这就说明数据卷挂载成功了。容器内的 Nginx 读取的是宿主机目录下的刚刚创建的 index.html 文件。

3. **查看容器内文件 (可选)**：在 Docker Desktop 中，点开正在运行的 `nginx-test` 容器，切换到 "Files" 标签页。

你应该能看到容器内的 `/usr/share/nginx/html` 目录被标记为 `mounted` (已挂载)，并且里面有我们的 `index.html` 文件：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100585559-638bed7c-bb06-492a-aceb-067b9e7a25b3.png)

如果你在挂载某些目录时遇到权限错误，可能需要在 Docker Desktop 的 Settings > Resources > File Sharing 里把你想要共享的宿主机目录添加进去：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748310966899-41fb0599-fb4b-49f6-b519-d6bac0b6cb16.png)

至于容器内应该挂载到哪个目录，通常镜像的文档（比如在 Docker Hub 上的描述页面）会说明。对于 Nginx，标准路径就是 `/usr/share/nginx/html`：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100746721-1f199bf6-e21e-450f-babb-233676d4f7c6.png)

通过命令行 docker run 来跑镜像， -v 是指定挂载的数据卷，后面的 :ro 代表 readonly，也就是容器内这个目录只读，:rw 表示容器内可以读写这个目录。

这就是数据卷的作用。

我们还可以进入容器内执行各种命令：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748100789370-f1e9a7f9-1098-452e-9cba-45ed15d7bf7c.png)



## 使用命令行操作
虽然 Docker Desktop 很方便，但在服务器上或者进行自动化部署时，命令行才是王道。上面图形化操作的每一步，都有对应的命令行指令：

### 拉取镜像 (pull)
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748312138676-c4e36b11-0b0d-45fa-a17b-dd6fb19a01bf.png)

```bash
docker pull nginx:latest
```

这里的 `nginx` 是镜像名，`latest` 是标签 (tag)，表示最新版本：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748101110401-e0d29ce2-320a-4473-b41b-942f2097d201.png)



### 运行镜像（run）
```bash
docker run --name nginx-test2 -p 81:80 -v /tmp/test:/usr/share/nginx/html -e KEY1=VALUE1 -d nginx:latest
```

我们来拆解一下这个命令：

+ `docker run`: 运行镜像以创建并启动一个新容器。
+ `--name nginx-test2`: 给容器命名为 `nginx-test2`。
+ `-p 81:80`: 将宿主机的 `81` 端口映射到容器的 `80` 端口。
+ ` -v /tmp/test:/usr/share/nginx/html`: 挂载数据卷。
    - `/tmp/test:/` 是宿主机路径 ( Mac 示例，Windows 请使用对应路径如 `D:\my-nginx-content`)。
    - `/usr/share/nginx/html` 是容器内路径。
    - 默认情况下，这种挂载是**可读写** （`rw`）的。如果希望容器内对这个挂载目录只有**只读权限**，可以在末尾添加 `:ro`，例如：`-v /tmp/test:/usr/share/nginx/html:ro`。
+ `-e KEY1=VALUE1`： 设置环境变量，容器内部的应用程序可以读取这个环境变量。
+ `-d`: 表示 `detached` 模式，即让容器在后台运行，不占用当前终端。
+ `nginx:latest`: 指定要使用的镜像及其标签。

docker run 会返回一个容器的 hash：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748143765819-6e7769e0-b4d5-4100-b0d1-aa58e5b97ca1.png)

就是这里的 id：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748143772982-b1d67996-cfce-4b4d-93c4-6e9850d354b6.png)

### 获取容器列表（ps）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1749039710026-45bbc9db-01ef-4ebe-a0c1-004109eee061.png)

上面容器列表界面可以用 docker ps 来获取：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748145301263-43cc4cdf-1e47-4a54-8c5d-58433436e4b3.png)

默认显示运行中的容器。

如果想显示全部，加个 -a

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748145311614-b7d6596e-7409-4118-950d-80df268a4bef.png)

### 获取镜像列表（images）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1749039736379-4a2f69d2-4afe-4a27-a6ce-17cf7f830305.png)

镜像列表可以通过 docker images 命令获取：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748145327982-bc9f0958-f562-4f99-a25a-3fdc48e62a54.png)

### 进入退出容器 terminal
我们在容器的 terminal 里执行命令：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748145405106-7453cfaa-e820-4d8c-a01a-570a25d57dba.png)

对应的是 docker exec 命令：

```bash
docker exec [选项] 容器名 命令 [参数]
```

[选项]：可选参数，用于调整命令行为：

+ `-i`：保持标准输入开启（交互模式）
+ `-t`：分配伪终端（通常与 `-i`一起使用）
+ `-d`：在后台运行命令
+ `-u`：指定执行用户（如 `-u root`）

容器名：必需参数，可以是容器名称或 ID

命令：必需参数，要在容器内执行的命令（如 `/bin/bash`、`ls` 等）

[参数]：可选参数，传递给命令的额外参数

比如以特定用户执行命令：`docker exec -u root mycontainer whoami`（会以 `root` 用户身份执行 `whoami` 命令）

比如进入容器交互终端：`docker exec -it my-nginx bash`（执行后会进入容器的 `bash shell`，输入 `exit` 可退出）

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155828874-ad5e9a26-24a8-4400-a722-2039fcede177.png)

退出容器 terminal：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155864319-5a23b58f-2e48-47ce-95d7-fe4a1745e68c.png)

### 查看日志（logs）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155885327-0a2159f1-9046-4697-b234-d97970a2bf1d.png)

对应 docker logs 命令：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155938007-4438de35-72a6-48a3-b31a-db98a3a7ef9d.png)

### 查看容器的详情（inspect）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155948456-6bccb519-63c9-4b85-8307-37a7ce3eb47a.png)

对应 docker inspect  命令：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155954089-255f75a1-ee82-4495-a39e-235100f95022.png)

### 推送镜像（push）
Docker 提供了 Docker Hub 镜像仓库，可以把本地镜像 push 到仓库，或从仓库 pull 镜像到本地：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748156009272-13c50599-eecc-491d-a8b2-1a9cdd8a05eb.png)

首先去 [Docker Hub](https://hub.docker.com/) 注册一个账户，登录后，输入用户名密码：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748155980761-826e211e-1de2-48aa-8029-4ac1c26cf24c.png)

#### 标记镜像 (tag)
在推送镜像之前，你需要为本地镜像打上一个包含**目标仓库地址**和**新标签**的新名称：

```bash
docker tag SOURCE_IMAGE[:TAG] [REGISTRY_HOST/][NAMESPACE/]TARGET_IMAGE[:TAG]
```

+ `SOURCE_IMAGE[:TAG]`：本地已有的镜像名称和标签（如 `my-app:latest`）。省略 `:TAG` 时默认为 `latest`
+ `[REGISTRY_HOST/]`：（可选）仓库地址
    - `docker.io`：Docker Hub（可省略）
    - `gcr.io`：Google Container Registry
    - `your-registry.com`：私有仓库
+ `[NAMESPACE/]`：（必需）命名空间/用户名/项目名
    - Docker Hub：用户名，如 `yourusername/`
    - 私有仓库：项目名，如 `my-project/`
+ `TARGET_IMAGE`：（必需）目标镜像名称
+ `[:TAG]`：（可选）标签，默认为 `latest`

示例：

```bash
# 标记为 Docker Hub 镜像
docker tag my-app:latest yourusername/my-app:1.0

# 标记为私有仓库镜像  
docker tag my-app:latest my-registry.com/my-project/my-app:prod

# 标记为 Google Container Registry 镜像
docker tag my-app:latest gcr.io/my-gcp-project/my-app:v2.1
```

#### 推送镜像（push）
首先确保已登录到目标仓库

```bash
# 登录 Docker Hub
docker login

# 登录私有仓库
docker login my-registry.com
```

标记完成后，推送镜像到远程仓库：

```bash
docker push [REGISTRY_HOST/][NAMESPACE/]TARGET_IMAGE[:TAG]
```

推送的镜像名称必须与标记时使用的名称完全一致。

示例：

```bash
docker push yourusername/my-app:1.0
docker push my-registry.com/my-project/my-app:prod
docker push gcr.io/my-gcp-project/my-app:v2.1
```

#### 拉取镜像（pull）
镜像推送成功后，其他用户可以拉取并使用该镜像：

```bash
docker pull [REGISTRY_HOST/][NAMESPACE/]TARGET_IMAGE[:TAG]
```

示例：

```bash
# 从 Docker Hub 拉取
docker pull yourusername/my-app:1.0

# 从私有仓库拉取（需要先登录）
docker pull my-registry.com/my-project/my-app:prod

# 拉取最新版本（省略标签）
docker pull yourusername/my-app
```

+ 公开镜像：任何人都可以拉取
+ 私有镜像：需要相应的访问权限和登录认证

### 管理数据卷（volume）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748158230314-3b85cda2-5611-44d3-817b-f342fc1ebc03.png)

使用 docker volume 命令：

![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748158236213-b96057e4-38aa-4542-87c2-9437d8d05bb8.png)

### 启动停止删除容器（start stop rm）
![](https://cdn.nlark.com/yuque/0/2025/png/21596389/1748158578959-3a43fc91-b5d5-43ab-b53d-530a0326be3d.png)

+ `docker start container_name_or_id` 启动一个已经停止的容器
+ `docker stop container_name_or_id` 停止一个容器
+ `docker rm container_name_or_id` 删除一个容器，如果正在运行，且你想强制删除：`docker rm -f container_name_or_id`



# 常用 Docker 命令一览
## 镜像管理
+ `docker pull <image_name>:<tag>` - 从仓库拉取镜像
+ `docker images` 或 `docker image ls` - 列出本地所有镜像
+ `docker rmi <image_id_or_name>` - 删除本地镜像
+ `docker tag <source_image> <target_image>` - 给镜像打标签
+ `docker push <image_name>:<tag>` - 推送镜像到远程仓库
+ `docker login` - 登录到 Docker 仓库
+ `docker build -t <image_name>:<tag> .` - 构建镜像
+ `docker history <image>` - 查看镜像构建历史
+ `docker save -o <file.tar> <image>` - 导出镜像到文件
+ `docker load -i <file.tar>` - 从文件导入镜像

## 容器管理
+ `docker run [OPTIONS] IMAGE [COMMAND]` - 创建并启动容器
+ `docker ps` - 列出运行中的容器
+ `docker ps -a` - 列出所有容器（包括已停止）
+ `docker start <container>` - 启动已停止的容器
+ `docker stop <container>` - 停止运行中的容器
+ `docker restart <container>` - 重启容器
+ `docker pause <container>` - 暂停容器
+ `docker unpause <container>` - 恢复暂停的容器
+ `docker rm <container>` - 删除已停止的容器
+ `docker rm -f <container>` - 强制删除容器（⚠️ 不推荐，会发送 SIGKILL 信号强行停止运行中的容器再删除，可能导致数据丢失或状态异常）
+ `docker logs <container>` - 查看容器日志
+ `docker logs -f <container>` - 实时跟踪日志
+ `docker exec -it <container> <command>` - 在容器内执行命令
+ `docker attach <container>` - 连接到运行中的容器
+ `docker inspect <container_or_image>` - 查看详细信息（JSON格式）
+ `docker stats` - 查看容器资源使用情况
+ `docker cp <src> <dest>` - 在容器和主机间复制文件
+ `docker rename <old_name> <new_name>` - 重命名容器

## 数据卷管理
+ `docker volume ls` - 列出所有数据卷
+ `docker volume create <volume_name>` - 创建数据卷
+ `docker volume rm <volume_name>` - 删除数据卷
+ `docker volume inspect <volume_name>` - 查看数据卷详情
+ `docker volume prune` - 删除未使用的数据卷

## 网络管理
+ `docker network ls` - 列出所有网络
+ `docker network create <network_name>` - 创建自定义网络
+ `docker network rm <network_name>` - 删除网络
+ `docker network inspect <network_name>` - 查看网络详情
+ `docker network connect <network> <container>` - 连接容器到网络
+ `docker network disconnect <network> <container>` - 断开容器网络连接
+ `docker network prune` - 删除未使用的网络

## 系统管理
+ `docker info` - 显示 Docker 系统信息
+ `docker version` - 显示版本信息
+ `docker system df` - 显示磁盘使用情况
+ `docker system prune` - 清理未使用的资源
+ `docker system prune -a` - 清理所有未使用的资源（包括镜像）
+ `docker system events` - 实时显示 Docker 事件



## Docker 实现原理
![画板](https://cdn.nlark.com/yuque/0/2025/jpeg/21596389/1748161630523-7bda3ca6-63f3-40e7-84dc-8fa660519eba.jpeg)

### Namespace：资源隔离机制
Namespace 可以让容器 A 里的进程以为自己的 PID 是 1，是系统老大，但其实在宿主机看来，它可能是 PID 12345。网络也是一样，每个容器都觉得自己有独立的网卡和 IP，但底层都是宿主机在统一管理。

Namespace 为容器提供进程级别的资源隔离，确保容器间互不干扰：

+ **PID Namespace**：进程 ID 空间隔离，容器内进程 ID 从 1 开始重新编号
+ **Network Namespace**：网络资源完全隔离，包括网络接口、路由表、防火墙规则等
+ **Mount Namespace**：文件系统挂载点隔离，容器拥有独立的文件系统视图
+ **IPC Namespace**：进程间通信隔离，包括信号量、消息队列、共享内存等
+ **UTS Namespace**：主机名和域名隔离，容器可设置独立的主机标识
+ **User Namespace**：用户和用户组 ID 映射隔离，实现权限安全控制
+ **Cgroup Namespace**：cgroups 根目录隔离，提供资源管理视图隔离

### cgroups：资源使用控制
光有隔离还不够，还得防止某个容器把资源全占了。cgroups 就像一个严格的管家，给每个容器分配固定的"配额"。

你的容器最多能用 2 个 CPU 核心、4GB 内存，超了就不行。这样就算某个应用有 bug 疯狂吃资源，也不会影响到其他容器。  
cgroups（Control Groups）负责限制和监控容器的资源使用：

+ **CPU 控制**：精确控制 CPU 使用率、时间片分配和调度优先级。
+ **内存管理**：限制内存使用上限，防止内存溢出（OOM）影响宿主机。
+ **存储 I/O**：控制磁盘读写带宽、IOPS 和访问优先级。
+ **网络带宽**：限制网络流量，确保网络资源公平分配。

### UnionFS：高效分层文件系统
这个最有意思。传统方式下，每个虚拟机都要完整复制一份操作系统，太浪费了。UnionFS 就像搭积木一样，底层是基础的 Ubuntu 系统，上面一层是 Java 运行环境，再上面一层是你的应用代码。

关键是，如果 10 个容器都用 Ubuntu，那这个 Ubuntu 层就只存一份，大家共享。只有当你要修改文件时，系统才会"写时复制"，把文件复制到你自己的层里改。这样既节省空间，启动也快。

UnionFS 实现了 Docker 镜像的高效存储和管理：

+ **分层存储架构**：镜像采用分层设计，每层仅存储与上层的增量变化
+ **写时复制机制**：文件修改时才进行实际复制，大幅减少存储开销
+ **层级共享优化**：多个容器可共享相同的基础镜像层，显著节省存储空间和启动时间

这三个技术配合起来就很神奇了：Namespace 让每个容器都以为自己是独立的系统，cgroups 确保资源使用不会失控，UnionFS 让存储高效又灵活。

结果就是，你能在几秒钟内启动一个"看起来像完整系统"的容器，但实际上它只是宿主机上的一个被精心包装的进程组而已。这就是为什么 Docker 比虚拟机轻量那么多的原因。


请你扮演一位 Go 项目的架构师，为一篇名为《Go 的代码组织与依赖管理：包与模块》的指南文章生成内容。

**文章目标：** 帮助开发者理解 Go 如何组织代码（包），以及如何管理项目依赖（模块），从而能够构建和维护可扩展、可复用的 Go 项目。

**内容要求：**

1.  **包 (Packages): 代码的基本单元**
    *   **核心概念**：解释“包”是 Go 组织代码的基本单位，同一个目录下的所有 `.go` 文件必须属于同一个包。
    *   **可见性规则 (Visibility)**：
        *   清晰地解释 Go 的可见性规则：**首字母大写**的标识符（变量、常量、函数、类型、方法等）可以被包外访问（导出，`public`）。
        *   **首字母小写**的标识符只能在包内访问（未导出，`private`）。
        *   提供一个清晰的示例，包含一个 `utils` 包和 `main` 包，展示如何导入和使用导出的函数。
    *   **包的导入 (Importing)**：
        *   讲解 `import` 语句的基本用法。
        *   介绍别名导入（`import alias "path"`）和点导入（`import . "path"`）以及它们的适用场景和潜在风险（特别是点导入）。
        *   解释下划线导入（`import _ "path"`）的用途：执行包的 `init` 函数而不导入其标识符，常用于数据库驱动或图片格式的注册。
    *   **`init` 函数**：解释 `init` 函数的特性（每个包可以有多个，在 `main` 函数前执行）和用途。

2.  **模块 (Modules): 依赖的管理方案**
    *   **从 `GOPATH` 到 Go Modules**：简要回顾历史，说明 Go Modules 是如何解决 `GOPATH` 模式下的“依赖地狱”问题的。
    *   **核心文件：`go.mod`**
        *   **`module`**：声明模块路径。
        *   **`go`**：指定 Go 版本。
        *   **`require`**：列出直接依赖及其版本。
        *   **`replace`**：将一个模块路径替换为另一个路径。**重点讲解其两个主要用途**：
            1.  调试本地尚未发布的模块。
            2.  使用私有仓库或 fork 的版本。
        *   **`retract`**：声明不应使用的版本。
    *   **`go.sum` 文件**：解释它的作用是记录每个依赖包的哈希值，以确保构建的完整性和可复现性。强调它应该被提交到版本控制中。
    *   **语义化版本控制 (Semantic Versioning)**：简要解释 `v1.2.3` (主版本.次版本.修订号) 的含义，以及 Go Modules 如何利用它来做依赖决策。

3.  **常用模块命令**
    *   **`go mod init [module-path]`**：初始化一个新模块。
    *   **`go mod tidy`**：**重点讲解**。解释它如何添加缺失的依赖并移除不再使用的依赖，是保持 `go.mod` 文件整洁的关键命令。
    *   **`go get [package@version]`**：获取或更新指定的依赖。
    *   **`go mod vendor`**：将所有依赖项的代码下载到项目根目录下的 `vendor` 文件夹中，用于离线构建。

4.  **常见问题**
    *   **循环依赖**：解释为什么 Go 不允许包之间存在循环依赖，以及如何通过重构代码来解决。
    *   **主版本号 > 1**：解释当你的模块主版本号升级到 `v2` 或更高时，模块路径需要包含版本号（如 `example.com/my/module/v2`）。

**写作风格：**
*   **概念清晰**：严格区分“包”和“模块”这两个术语。
*   **历史视角**：通过回顾 `GOPATH` 的历史，让读者更好地理解 Go Modules 的价值。
*   **实践驱动**：重点放在 `go.mod` 文件和 `go mod` 命令的实际应用上。
*   **结构分明**：使用清晰的标题和列表来组织内容。

请生成这篇文章的完整 Markdown 内容。
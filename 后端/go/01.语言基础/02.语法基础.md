请你扮演一位资深的 Go 语言导师，为一篇名为《Go 语言核心语法快速入门》的教程生成内容。

**文章目标：** 帮助编程初学者或从其他语言转向 Go 的开发者快速掌握 Go 的核心语法。

**内容要求：**

1.  **变量 (Variables):**
    *   讲解标准声明 (`var name type`) 和类型推断 (`var name = value`)。
    *   重点介绍并举例说明短变量声明 (`name := value`) 及其作用域规则。
    *   提供批量声明变量的示例。

2.  **常量 (Constants):**
    *   解释常量的定义 (`const`)。
    *   介绍 `iota` 的用法，并通过示例说明其在枚举中的应用。

3.  **基础数据类型 (Basic Data Types):**
    *   简要介绍 Go 的主要基础类型，包括：布尔型 (`bool`)、整型 (`int`, `int8`, ... `uint`), 浮点型 (`float32`, `float64`), 和字符串 (`string`)。
    *   强调 Go 是强类型语言，类型转换必须是显式的。

4.  **控制流程 (Control Flow):**
    *   **`if-else`**：包括 `if` 的初始化语句（如 `if err := doSomething(); err != nil`）。
    *   **`for` 循环**：展示 Go 中唯一的循环结构 `for` 的三种形式（类 `while`、无限循环、标准 `for`）。
    *   **`switch`**：讲解 `switch` 的基本用法，以及 `fallthrough` 和无表达式 `switch`（作为 `if-else` 链的替代）。

5.  **复合数据结构 (Composite Data Structures):**
    *   **数组 (Arrays):** 解释其定长特性，并提供声明和初始化的示例。
    *   **切片 (Slices):**
        *   重点讲解切片是对底层数组的引用。
        *   介绍如何使用 `make` 创建切片，以及 `len()` 和 `cap()` 的区别。
        *   提供 `append` 函数的使用示例，并解释其可能触发的底层数组扩容。
        *   展示切片的截取操作。
    *   **`map`:**
        *   讲解如何创建、初始化、插入、获取和删除键值对。
        *   演示如何安全地从 map 中取值（使用 "comma ok" idiom）。
        *   展示如何遍历 map。

6.  **指针 (Pointers):**
    *   解释指针是什么，以及为什么在 Go 中使用指针（例如，修改函数外部的变量、传递大的数据结构）。
    *   演示如何使用 `&` 获取地址和 `*` 解引用。
    *   强调 Go 的指针是类型安全的，不支持指针运算。

**写作风格：**
*   **循序渐进**：从最简单的概念开始，逐步深入。
*   **代码驱动**：每个知识点都必须配有简洁、可运行的 Go 代码示例。
*   **要点突出**：对于 Go 特有的或容易混淆的概念（如短变量声明、切片与数组的区别、`iota`），要重点解释。
*   **语言精炼**：直接、准确地描述概念，避免冗长。

请生成这篇文章的完整 Markdown 内容。
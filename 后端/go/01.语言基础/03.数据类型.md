请你扮演一位 Go 语言专家，为一篇名为《深入理解 Go 语言的数据类型》的技术文章生成内容。

**文章目标：** 全面、深入地介绍 Go 语言的各种数据类型，帮助开发者理解其设计哲学和在实际开发中的应用。

**内容要求：**

1.  **类型系统哲学：**
    *   简要介绍 Go 的静态、强类型特性。
    *   解释“类型是代码的基石”这一理念。

2.  **基础类型 (Recap & Deep Dive):**
    *   回顾布尔、数值（整型、浮点型、复数）和字符串类型。
    *   **深入点**：
        *   字符串的内部表示（只读的字节切片）。
        *   `rune` 类型及其与 `int32` 的关系，用于处理 Unicode 字符。
        *   不同数值类型之间的转换规则和注意事项。

3.  **复合类型 (Composite Types):**
    *   **数组 (Array):**
        *   强调其“值类型”特性，赋值和传参时会发生拷贝。
        *   讨论其适用场景（通常不直接使用，而是作为切片的底层存储）。
    *   **切片 (Slice):**
        *   详细解释其内部结构：指针、长度（`len`）、容量（`cap`）。
        *   通过图示或生动的例子说明 `append` 如何影响长度和容量，以及何时会触发底层数组的重新分配。
        *   讲解切片作为函数参数的行为（引用传递的效果）。
    *   **`map`:**
        *   解释其内部实现（哈希表）。
        *   强调其并发读写是不安全的，需要加锁保护。
        *   讨论 `map` 的遍历顺序是不确定的。
    *   **结构体 (Struct):**
        *   讲解如何定义和使用结构体来组织数据。
        *   介绍结构体的内存布局和对齐概念。
        *   解释匿名字段（嵌入）和标签（Tag）的用法，特别是在 JSON 编码/解码中的应用。

4.  **特殊类型：**
    *   **指针 (Pointer):**
        *   重申其核心作用：共享数据和修改数据。
        *   讨论 `new` 和 `make` 的区别：`new` 用于分配内存并返回指针，主要用于值类型；`make` 用于初始化切片、map 和通道。
    *   **接口 (Interface):**
        *   介绍接口是方法的集合，是 Go 实现多态的核心。
        *   解释空接口 `interface{}` 的作用及其类型断言。
    *   **通道 (Channel):**
        *   简要介绍其作为并发通信原语的作用（详细内容将在并发章节展开）。
    *   **函数类型 (Function Type):**
        *   解释函数在 Go 中是一等公民，可以作为变量、参数和返回值。

**写作风格：**
*   **深度与广度并存**：既要覆盖所有类型，又要对关键类型（如切片、结构体）进行深入剖析。
*   **图文并茂**：建议使用简单的图示来解释切片的内部结构或指针的指向。
*   **场景驱动**：结合实际开发场景来解释为什么需要某种数据类型（例如，为什么用切片而不是数组）。
*   **专业准确**：使用精确的技术术语，并对关键概念进行清晰的定义。

请生成这篇文章的完整 Markdown 内容。
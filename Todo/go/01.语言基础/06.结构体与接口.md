请你扮演一位 Go 语言的设计模式专家，为一篇名为《Go 的两大支柱：结构体与接口》的深度解析文章生成内容。

**文章目标：** 帮助开发者深刻理解结构体（Structs）如何组织数据，以及接口（Interfaces）如何定义行为，并最终领会它们如何协同工作，构建出灵活、可扩展的 Go 程序。

**内容要求：**

1.  **结构体 (Structs): 数据的组织者**
    *   **基础**：回顾结构体的定义、实例化（包括字面量和 `new`）以及字段的访问。
    *   **组合与嵌入 (Composition and Embedding)**：
        *   解释 Go 如何通过“组合优于继承”的理念来构建复杂类型。
        *   通过示例清晰地展示匿名字段（嵌入）如何实现类似“继承”的效果，但本质上是组合。
    *   **标签 (Tags)**：
        *   详细讲解结构体标签的作用，并以 `json` 标签为例，展示如何通过反射（reflection）在运行时读取标签信息，从而影响程序的行为（如 JSON 序列化/反序列化）。

2.  **接口 (Interfaces): 行为的定义者**
    *   **核心哲学**：
        *   开篇明义：“如果一个东西走起来像鸭子，叫起来也像鸭子，那它就是一只鸭子。” —— 解释 Go 的非侵入式接口（隐式实现）哲学。
        *   强调接口定义的是“行为”（能做什么），而不是“类型”（是什么）。
    *   **定义与实现**：
        *   讲解如何定义一个接口（方法的集合）。
        *   展示任何类型只要实现了接口中所有的方法，就被视为实现了该接口。
    *   **空接口 (`interface{}`)**：
        *   解释空接口可以表示任何类型。
        *   讲解如何使用类型断言（`value, ok := i.(T)`）和类型选择（`switch i.(type)`）来安全地处理空接口变量。
    *   **接口值 (Interface Values)**：
        *   **深入点**：通过图示或清晰的描述，解释一个接口变量在内部由两部分组成：一个指向具体类型信息的指针和一个指向具体值的指针。
        *   **陷阱**：解释为什么一个值为 `nil` 的指针赋给接口后，该接口不等于 `nil`。这是 Go 中一个常见的坑。

3.  **结构体与接口的协同工作：Go 的多态**
    *   **多态示例**：通过一个经典的例子（如 `Shape` 接口，以及 `Circle` 和 `Rectangle` 结构体实现），展示如何编写一个接受接口类型参数的函数（如 `calculateTotalArea`），该函数可以处理任何实现了该接口的具体类型。
    *   **接口作为契约**：强调接口在大型项目和团队协作中的重要性——它定义了模块之间清晰的边界和契约。
    *   **小接口原则**：介绍 Go 的设计哲学——倾向于定义小而专一的接口（如 `io.Reader`, `io.Writer`），这使得类型可以更容易地实现多个接口，增加了代码的灵活性和可复用性。

**写作风格：**
*   **思想深刻**：不仅仅是罗列语法，更要传递 Go 的设计哲学和思想。
*   **类比生动**：使用生动的比喻（如“鸭子类型”）来解释抽象的概念。
*   **图文并茂**：强烈建议使用图示来解释接口值的内部结构或组合与继承的区别。
*   **代码精炼**：示例代码应专注于核心概念，避免不必要的复杂性。

请生成这篇文章的完整 Markdown 内容。
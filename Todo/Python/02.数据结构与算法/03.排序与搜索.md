# Python 排序与搜索

## 最佳提示词

```
请详细介绍 Python 的排序与搜索算法，包括以下内容：

1. 排序算法：
   - 冒泡排序、选择排序、插入排序
   - 快速排序、归并排序、堆排序
   - 计数排序、桶排序、基数排序
   - Python 内置排序函数的使用
   - 排序算法的性能比较

2. 搜索算法：
   - 线性搜索、二分搜索
   - 深度优先搜索（DFS）
   - 广度优先搜索（BFS）
   - 哈希表搜索

3. 算法应用：
   - 查找第 K 大/小元素
   - 数组中的重复元素
   - 搜索旋转排序数组
   - 区间搜索问题

请为每个算法提供详细的代码实现，包括时间复杂度分析和实际应用场景。
```

## 学习要点

- 掌握常见排序算法的实现原理
- 理解不同排序算法的适用场景
- 学会使用 Python 内置排序功能
- 掌握二分搜索的实现和应用
- 理解 DFS 和 BFS 的遍历策略
- 学会分析算法的时间和空间复杂度
- 了解排序算法的稳定性概念

## 实践练习

1. 实现各种排序算法并比较性能
2. 使用二分搜索解决查找问题
3. 应用 DFS 和 BFS 解决图遍历问题
4. 实现查找第 K 大元素的算法
5. 解决旋转数组的搜索问题
6. 优化现有算法的性能

## 代码示例模板

```python
# 快速排序
def quick_sort(arr):
    """快速排序实现"""
    if len(arr) <= 1:
        return arr
    
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quick_sort(left) + middle + quick_sort(right)

# 二分搜索
def binary_search(arr, target):
    """二分搜索实现"""
    left, right = 0, len(arr) - 1
    
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1

# 深度优先搜索
def dfs(graph, start, visited=None):
    """深度优先搜索"""
    if visited is None:
        visited = set()
    
    visited.add(start)
    print(start, end=' ')
    
    for neighbor in graph[start]:
        if neighbor not in visited:
            dfs(graph, neighbor, visited)

# 广度优先搜索
from collections import deque

def bfs(graph, start):
    """广度优先搜索"""
    visited = set()
    queue = deque([start])
    visited.add(start)
    
    while queue:
        vertex = queue.popleft()
        print(vertex, end=' ')
        
        for neighbor in graph[vertex]:
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append(neighbor)
```

## 扩展资源

- [Python 排序算法](https://docs.python.org/3/howto/sorting.html)
- [算法可视化](https://www.cs.usfca.edu/~galles/visualization/Algorithms.html)
- [LeetCode 排序题目](https://leetcode.com/tag/sorting/)
- [搜索算法详解](https://en.wikipedia.org/wiki/Search_algorithm)
# Python 类与对象

## 最佳提示词

```
请详细介绍 Python 的类与对象，包括以下内容：

1. 类的基础概念：
   - 类的定义与创建
   - 对象的实例化
   - 属性与方法
   - 构造函数与析构函数
   - self 参数的理解

2. 类的属性：
   - 实例属性
   - 类属性
   - 私有属性与公有属性
   - 属性装饰器

3. 类的方法：
   - 实例方法
   - 类方法
   - 静态方法
   - 方法的重载与重写

4. 对象的生命周期：
   - 对象的创建过程
   - 对象的使用
   - 对象的销毁
   - 内存管理

请为每个概念提供详细的代码示例，包括实际应用场景和最佳实践。
```

## 学习要点

- 理解类和对象的基本概念
- 掌握类的定义和对象的创建
- 学会使用实例属性和类属性
- 理解私有属性和公有属性的区别
- 掌握不同类型方法的定义和使用
- 了解对象的生命周期管理
- 学会使用属性装饰器

## 实践练习

1. 创建一个简单的 Person 类
2. 实现实例属性和类属性的使用
3. 练习私有属性的定义和访问
4. 创建实例方法、类方法和静态方法
5. 使用属性装饰器控制属性访问
6. 观察对象的生命周期

## 代码示例模板

```python
# 类的定义
class Person:
    """人类"""
    species = "Homo sapiens"  # 类属性
    
    def __init__(self, name, age):
        """构造函数"""
        self.name = name        # 实例属性
        self.age = age
        self.__private_data = "私有数据"  # 私有属性
    
    def greet(self):
        """实例方法"""
        return f"Hello, I'm {self.name} and I'm {self.age} years old."
    
    @classmethod
    def get_species(cls):
        """类方法"""
        return cls.species
    
    @staticmethod
    def is_adult(age):
        """静态方法"""
        return age >= 18
    
    @property
    def private_data(self):
        """属性装饰器"""
        return self.__private_data
    
    def __del__(self):
        """析构函数"""
        print(f"{self.name} 对象被销毁")

# 使用示例
person = Person("Alice", 25)
print(person.greet())
print(Person.get_species())
print(Person.is_adult(20))
print(person.private_data)
```

## 扩展资源

- [Python 类文档](https://docs.python.org/3/tutorial/classes.html)
- [面向对象编程指南](https://realpython.com/python3-object-oriented-programming/)
- [Python 属性装饰器](https://docs.python.org/3/library/functions.html#property)
- [Python 内存管理](https://docs.python.org/3/c-api/memory.html)